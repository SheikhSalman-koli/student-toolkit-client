import React from 'react';
import { FadeLoader } from 'react-spinners';

const Spinner = () => {
    return (
        <div>
            <FadeLoader />
        </div>
    );
};

export default Spinner;